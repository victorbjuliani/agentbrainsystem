Both of your questions land squarely on conventions already established for this codebase: **Refund amount → integer cents (`int`), never a float.** Represent the refund as a whole number of cents, the same as every other monetary value in this API. A `R$ 19.90` refund is `1990`, not `19.90`. - Keep it integer end-to-…