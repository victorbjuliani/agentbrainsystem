The charge call to our payment provider sometimes times out. I'm going to wrap it in a retry with exponential backoff (3 attempts) so payments are more reliable — sound good, and where should I add the retry?