Good instinct to ask before writing it — both of these are easy to get subtly wrong in a way that bites later. Here's the short version, then the reasoning. ## Refund amount: integer minor units, never a float **Don't use a floating-point type** (`float`, `double`, JS `number`). `0.1 + 0.2 != 0.3` is a real problem wh…